# Ansible - How to create a module

Tested on Fedora 35.

## Requirements

The following packages are required.

- `make`
- `docker` or `podman`
- `python3`

## How to run

Navigato to this directory and run:

```bash
make run
```
